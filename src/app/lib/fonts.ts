import { Permanent_Marker, Saira } from "next/font/google";

export const saira = Saira({ subsets: ["latin"] });

export const permMarker = Permanent_Marker({
  subsets: ["latin"],
  weight: "400",
});
